# YorAkka V3.23

## V3.23 changes

- Fixes the My Matches condition column so labels such as Correct score 2–1 use the available space instead of being unnecessarily clipped.
- Removes the Test red-card indicator developer/test card from Settings.
- Expands the common Correct Score shortcuts to 18 choices, arranged as a balanced grid on wider screens.
- Keeps the touch score wheels for any scoreline not shown as a shortcut.
- Updates the visible version and service-worker cache to V3.23.

Replace all GitHub Pages files with the contents of this package. The Cloudflare Worker is unchanged.
